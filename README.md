# TileSHowcase

Luxury tile showroom app to browse tiles, compare options, and build quick printable quotes.

## Run locally

```bash
cd tileshowcase
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Update tile data

Edit `tileshowcase/src/lib/tiles.ts`.
